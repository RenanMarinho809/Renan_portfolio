export interface Project {
    title: string;
    description: string;
    tags: string[];
    deployLink: string;
    repoLink: string;
    image: string;
}

export interface Tech {
    name: string;
    imgSrc: string;
}